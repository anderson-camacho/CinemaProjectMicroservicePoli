spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: file:///C:/Config
  security:
    user:
      name: admin
      password: admin
server:
  port: 9000